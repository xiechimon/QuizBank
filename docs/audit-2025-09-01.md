# QuizBank Flutter 审视与增删清单 — 2025-09-01

> 审视人：Pi Agent（全库只读 + iOS 事实源对照）  
> 事实源：`AppleProjects/QuizBank/CONTEXT.md` + `QuizBank/{App,Core,Features,Models}` vs `AndroidProjects/QuizBank/lib/**`  
> 基线：`flutter analyze 0 issue` · `flutter test 68 passed` · `pubspec 1.0.0+1`

## 1. 结论
Flutter 版是「题库驱动的可用骨架」，iOS 版是「考点驱动的完整闭环」。**P0 缺 3 项，不补上架会被视为阉割版**。

## 2. 该加（按 P0/P1/P2）

### P0 — 不加不算重构完成
| 编号 | 功能 | 现状 | 补齐方案 | 工时 |
|---|---|---|---|---|
| P0-1 | AI解析全链路 | `ai_explain_service.dart` 32KB 已实现 SSE/401/占位，但零 UI 调用 | `QuestionBodyView`/`StudyCardContent` 改 `SelectableText` + 选区菜单「AI解析」→ `AIExplainSheet`（Markdown流式，mimo增量/longcat降级，断流失败） | 1-1.5d |
| P0-2 | 速记卡富文本 | `Text(card.content)` 裸 dump，`\|·#【】` 语法丢失 | 移植 `CardCleaning`/`StudyCardContentFormatter` → `card_content_parser.dart` + `CardContentView`（段落/列表/表格/强调，heading不可选） | 0.5-1d |
| P0-3 | 术语与设置单源 | 无 `CONTEXT.md`，`settings_view.dart` 自带 `AiConfig(ai.apiKey)` 与真实 `AIConfig(ai.opencode.key)` 双源，保存后仍走 placeholder | 新增 `CONTEXT.md`（已落盘），删除重复类，设置页直连 `AIConfig.instance` | 0.5d |

### P1 — 体验/正确性
| 编号 | 功能 | 改法 |
|---|---|---|
| P1-1 | TTS磨耳朵 | `flutter_tts` + `CardReadingService`（行级高亮/翻页续播/断点）— 按需，审计通勤场景可升 P0 |
| P1-2 | 错题本判定 | `timesWrong>0` → `timesWrong>0 && phase<6` |
| P1-3 | 今日进度条 | `dueQuestions/totalQuestions` → `dueCards/totalCards` 或题数比修正 |
| P1-4 | 路由与依赖 | `go_router` 0引用：要么实现 `GoRouter(/today/quiz)` + `-initialTab` 调试参数，要么删除；`flutter_markdown` → `flutter_markdown_plus` |

### P2 — 延后
双轨调度说明、StatsView 拆 6 子件、进度导出、深链参数。

## 3. 该删（减负/包体积）

| 删除项 | 位置 | 理由 | 收益 |
|---|---|---|---|
| docx asset | `pubspec.yaml assets: ...docx` | 388KB 打进 apk 无人读，工具链本地读即可 | 体积 |
| 重复 AiConfig | `features/settings/settings_view.dart:1-60` | 与 `features/ai/ai_config.dart` 双源致保存失效 | 正确性 |
| `go_router`/`cupertino_icons` 二选一 | `pubspec.yaml` | 0 引用 | 体积/噪音 |
| `daily_plan.dart` 生产引用 | `core/daily_plan.dart` | Today已切 `Derived`，仅测试用，移 `test/helpers` | 认知 |
| `database.g.dart` 提交 | 若建 git | 生成文件，`.gitignore: *.g.dart` | 冲突 |

## 4. 该改（重塑）

1. **Favorite 同步**：详情页 `card.isFavorite=!` 后未 `invalidate(allStudyCardsProvider)`，列表不同步。
2. **StatsView 509行**：拆 `overall/mastery/type/daily/forecast/topic` 6 件。
3. **导入失败静默**：`main.dart catch debugPrint` → `FutureBuilder` error 态重试按钮。
4. **SchemaVersion**：当前 1 无 `onUpgrade`，后续加列炸库；补 migrator 脚手架。

## 5. 路线图

```
P0 1周：AI Sheet + 卡片富文本 + 设置单源/CONTEXT
P1 1周：错题本fix/进度fix/路由取舍/markdown迁移/docx去asset
P2 按需：TTS/导出
```

## 6. 验证

- `flutter analyze` 0 issue（已通过）  
- `flutter test 68` 全绿（含 `importer 1170`/`AI 401断流`/`Derived 40题`）  
- 手测：长按AI解析流式、卡片表格横滑、今日队列约40题、错题本进出

## 7. 下一步

`/grill-with-docs` 压 3 ADR → `/to-spec` → `/to-tickets` 5 张 tracer（阻塞边：AI-sheet→TTS）→ `/implement` TDD。
